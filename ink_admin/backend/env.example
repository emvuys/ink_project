# ===========================================
# LOCAL DEVELOPMENT (use this for testing locally)
# ===========================================
# DATABASE_URL="postgresql://postgres:password@localhost:5432/ink_admin"

# ===========================================
# GOOGLE CLOUD SQL (Production)
# ===========================================
# Option 1: Direct connection (requires Cloud SQL Proxy or public IP)
# DATABASE_URL="postgresql://USER:PASSWORD@PUBLIC_IP:5432/ink_admin"

# Option 2: Unix socket connection (for Cloud Run)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost/ink_admin?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME"

# Example for Cloud SQL:
# DATABASE_URL="postgresql://ink_admin_user:YourSecurePassword@/ink_admin?host=/cloudsql/ink-project-123:us-central1:ink-admin-db"

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/ink_admin"

# ===========================================
# JWT Configuration
# ===========================================
# Generate with: openssl rand -base64 32
JWT_SECRET="change-this-to-a-secure-random-string-at-least-32-chars"
JWT_EXPIRES_IN="7d"

# ===========================================
# Server Configuration
# ===========================================
PORT=3001
NODE_ENV="development"

# For production, set to your Firebase Hosting URL
ALLOWED_ORIGINS="http://localhost:5173,https://your-app.web.app"

# ===========================================
# File Upload
# ===========================================
MAX_FILE_SIZE=1048576
UPLOAD_DIR="uploads"

# ===========================================
# Google Cloud Storage (for video uploads in production)
# ===========================================
GCS_BUCKET_NAME="ink-admin-videos"
GCS_PROJECT_ID="your-firebase-project-id"
# GCS_KEYFILE_PATH="./service-account-key.json"
